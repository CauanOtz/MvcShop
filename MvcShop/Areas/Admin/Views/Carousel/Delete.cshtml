@model MvcShop.Models.CarouselImage

@{
    ViewData["Title"] = "Excluir Imagem do Carousel";
}

<h2>Excluir Imagem</h2>

<div class="card mb-3">
    <div class="card-body">
        <img src="@Model.ImageUrl" class="img-fluid mb-3" style="max-height:200px;object-fit:cover;" />
        <h5>@Model.Title</h5>
        <p>Ordem: @Model.SortOrder</p>

        <form asp-action="Delete" method="post">
            <input type="hidden" asp-for="Id" />
            <button class="btn btn-danger">Confirmar exclusão</button>
            <a class="btn btn-secondary ms-2" href="/Admin/Carousel">Cancelar</a>
        </form>
    </div>
</div>
