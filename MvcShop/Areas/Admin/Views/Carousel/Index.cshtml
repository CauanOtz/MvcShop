@model IEnumerable<MvcShop.Models.CarouselImage>

@{
    ViewData["Title"] = "Admin - Carousel";
}

<h2>Carousel</h2>
<p><a class="btn btn-primary" href="/Admin/Carousel/Create">Adicionar Imagem</a></p>

<table class="table">
    <thead><tr><th>Preview</th><th>Título</th><th>Ordem</th><th></th></tr></thead>
    <tbody>
    @foreach(var it in Model)
    {
        <tr>
            <td style="width:200px;"><img src="@it.ImageUrl" class="img-fluid" style="max-height:80px;object-fit:cover;" /></td>
            <td>@it.Title</td>
            <td>@it.SortOrder</td>
            <td>
                <a class="btn btn-sm btn-danger" href="/Admin/Carousel/Delete/@it.Id">Excluir</a>
            </td>
        </tr>
    }
    </tbody>
</table>
